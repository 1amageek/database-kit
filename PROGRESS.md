# Progress

- [x] DKF-01 Establish one authoritative `database-kit` design hierarchy conforming to workspace `SPEC.md` and `DESIGN.md`: the package owns Foundation-independent database declarations and deterministic bounded Wire contracts but no storage, authority, transaction, planning, execution, transport, or application schema; the default graph is target-free DatabaseWire v3 and only the non-default `MultiBase` trait is target-bound DatabaseWire v5; complete one coherent design commit after every child and original task-designer conformance review `depends:none` `parallel:none`
  - [x] DKF-01.1 Create package-root `DESIGN.md` with the required purpose, ownership boundary, related-design index, architecture, contracts, lifecycle/failure constraints, and verification/change-impact sections; index the five production modules without duplicating their internal contracts, and treat root `SPEC.md` as the product authority `depends:none` `parallel:none`
  - [x] DKF-01.2 Create module-root `DESIGN.md` authorities for `DatabaseKit`, `DatabaseKitMacros`, `DatabaseWire`, `DatabaseKitFoundation`, and `DatabaseSchemaJSON`, each naming parent, children, dependencies, dependents, assumptions, guarantees, public boundary, and owned behavioral evidence; keep declaration validation in Kit/Macros, v3/v5 framing and decode bounds in Wire, and native-only adaptation out of the canonical and Embedded graphs `depends:DKF-01.1` `parallel:dkf-design-authority`
  - [x] DKF-01.3 Replace the two superseded independent authorities `Docs/DATABASE_KIT_SPECIFICATION.md` and `Docs/ARCHITECTURE.md` with the new hierarchy, update README and workspace `DESIGN.md` links to the package authority, and prove no remaining link or normative statement reintroduces Wire v1, a mandatory target in the standard graph, unconditional Base/Composition/Grant, or a second owner for package/module meaning `depends:DKF-01.1` `parallel:dkf-design-authority`

- [x] DKF-02 Fix and freeze the Directory declaration macro boundary without changing the public macro signatures or moving runtime interpretation into DatabaseKit; complete one coherent implementation commit after focused behavioral verification and original task-designer conformance review `depends:DKF-01` `parallel:none`
  - [x] DKF-02.1 Make `#Directory` and `@PolymorphicDirectory` accept only nonempty ordinary single-line string literals without interpolation for static path components, reject raw and multiline forms before generated code can reinterpret their bytes, and keep the broader static-literal helper used by unrelated contracts such as `@Polymorphable(identifier:)` behaviorally unchanged `depends:DKF-01` `parallel:none`
  - [x] DKF-02.2 Admit `layer:` only when it is `.default`, `.partition`, `DirectoryLayer.default/partition`, or `DatabaseKit.DirectoryLayer.default/partition`; reject foreign member bases and preserve the admitted expression as written so validation cannot change name resolution or forge a canonical spelling `depends:DKF-02.1` `parallel:none`
  - [x] DKF-02.3 Preserve the executed foreign-base and raw-literal failures as the red defect contract, then run the Directory parser, Persistable diagnostic, and Polymorphable expansion suites once green with explicit success and rejection coverage for ordinary, raw, multiline, interpolated, empty, qualified, module-qualified, foreign-base, repeated-label, and unrelated raw-identifier behavior `depends:DKF-02.2` `parallel:none`

- [ ] DKF-03 Encode the frozen verification contract in package-owned documentation, harness, and CI without changing public API or enabling `MultiBase` by default; complete one coherent verification-infrastructure commit after source/diff review `depends:DKF-01,DKF-02` `parallel:none`
  - [ ] DKF-03.1 Pin README, CI, and the native harness to Swift `swift-6.4.x-DEVELOPMENT-SNAPSHOT-2026-08-14-a` / `org.swift.64202608141a`; make the harness own the reviewed post-regression counts of 641 standard tests and 656 `MultiBase` tests with zero failures, skips, expected failures, runtime warnings, compiler/plugin internal errors, profile errors, or debug-information verification warnings `depends:DKF-01,DKF-02` `parallel:none`
  - [ ] DKF-03.2 Make CI execute isolated standard and `MultiBase` native lanes with distinct result artifacts, plus release builds of `DatabaseKit` and `DatabaseWire` under the exact matching standard and Embedded WASM SDKs and the Embedded `DatabaseKitDeclarationContract` macro fixture, all with URL-only dependencies, default traits unchanged, timeouts, and `-debug-info-format none` `depends:DKF-03.1` `parallel:none`

- [ ] DKF-90 Verify the exact accumulated `database-kit` candidate once and accept it only when the design hierarchy is link-complete and contradiction-free, the focused macro behavior is green, the native standard lane reports exactly 641/641 and the isolated `MultiBase` lane exactly 656/656 with every strict zero condition, all five standard/Embedded WASM release builds succeed on the pinned matching SDKs, `Package.swift` remains URL-only with no unintended default trait, `git diff --check` and the incomplete-implementation audit pass, and the original task designer reviews the actual implementation paths and preserved artifacts; then commit any evidence-owned correction, require every ahead commit to be task-owned, push normal `main` to its configured upstream, and prove a clean `main == origin/main` without claiming that the scope-external database-framework polymorphic runtime path is complete `depends:DKF-01,DKF-02,DKF-03` `parallel:none`
